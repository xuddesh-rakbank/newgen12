// ADD HERE — just before the if(CheckGridDataMap.get("UpdProspect_Reqd")...) line
String salariedEmployerTags = "";
if ("Salaried".equalsIgnoreCase(emp_type)) {
    salariedEmployerTags =
        "\t\t\t" + "<employerindustry>" + CheckGridDataMap.get("industry_val") + "</employerindustry>" + "\n" +
        "\t\t\t" + "<employersubcategory>" + CheckGridDataMap.get("SubCategory_val") + "</employersubcategory>" + "\n";
}
